# Jakarta camera DHCP reservation
# Replace <CAM1_MAC> with the actual camera MAC address
# (printed on the camera label, or found via: cat /var/lib/misc/dnsmasq.leases)
# dhcp-host=<CAM1_MAC>,192.168.50.101,cam1
