# Grant dialout group access to Pi UART (ttyAMA*) for RG-15 rain gauge.
# Default kernel permissions are 0600 root:root which blocks non-root access.
KERNEL=="ttyAMA[0-9]*", GROUP="dialout", MODE="0660"
